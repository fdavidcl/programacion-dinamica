#!/usr/bin/env python
# encoding: utf8

from graph import *

def floyd(graph):
    n = graph['num_vertices']

    for i in range(n):
        for j in range(n):
            for k in range(n):
                

    return matrix


if __name__ == "__main__":
    matrix = distanceMatrix(readGraph())
    printMatrix(matrix)
